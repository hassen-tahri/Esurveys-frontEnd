import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esurveys.app',
  appName: 'esurveys',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
